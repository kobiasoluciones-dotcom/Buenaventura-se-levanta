# URL pública del backend Express, sin barra final.
# El navegador solo consume sus rutas GET /api; las credenciales de Supabase
# permanecen exclusivamente en el backend.
NEXT_PUBLIC_API_BASE_URL=https://api.example.com
